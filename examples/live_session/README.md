# live_session

Progressive live query example for Portolan.

This example shows the session-based retrieval path:

- one lifted snapshot source
- one progressive live source
- one coordinated session
- incremental patch events with stable ids

Run it with:

```bash
cargo run -p live_session
```
